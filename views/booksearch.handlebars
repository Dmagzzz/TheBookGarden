<div class="row">
  <div class="col-md-auto">
    <h2>Welcome, {{name}}!</h2>
  </div>
</div>

   <form class="d-flex" role="search">
    <input id="searchInput" class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
    <button id="searchBtn" class="btn btn-outline-dark" type="submit">Search</button>
  </form>

  <div id="bookList"></div>

<!--div class="row mt-4">
  <div class="col-md-6">
    <h3>Loading Books....</h3>
  {{!-- Create a list group for each of the books to be displayed on the page. --}}
    <ol id="bookList">

    </ol>
    <p class="hello"></p>
  </div>

  {{#if book.length}}
  <div class="col-md-6 book-list">
    <h3>Current Books:</h3>

    {{#each book as |book|}}
    <div class="row mb-2">
      <div class="col-md-8">
        <h4><a href="/book/{{book.id}}">{{book.name}}</a></h4>
      </div>
      <div class="col-md-4">
        <button class="btn btn-sm btn-danger" data-id="{{book.id}}">DELETE</button>
      </div>
    </div>
    {{/each}}
  </div>
  {{/if}}
</div-->

<script src="/js/booksearch.js"></script>
