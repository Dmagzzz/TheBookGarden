<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{author}}</p>
  <p>{{is_read}}</p>
  <p>{{description}}</p>
  <p>Finished reading {{user.name}} on {{format_date date_created}}</p>
  <p>
    <span for="img" aria-label="money">💲</span>
    <span class="dollar-amount">Written by {{book_author}}</span>
  </p>
</div>
